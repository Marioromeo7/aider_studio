# Screenshots

Add listing images here (referenced from the main README via absolute raw URLs):

- `chat.png` — the chat sidebar with a streamed answer
- `diff.png` — an inline accept/undo diff card
- `provider.png` — the "Add custom provider" modal
- (optional) `demo.gif` — a 10–15s end-to-end clip

PNG, ~1200px wide. On the Marketplace, only absolute HTTPS image URLs render.
